/**
 * op_add - add two integer numbers
 * @a: first number
 * @b: second number.
 *
 * Return: sum a and b.
 */

int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - sub two integer numbers
 * @a: first number
 * @b: second number.
 *
 * Return: difference between integer numbers
 */

int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - multiply two integer numbers
 * @a: first number
 * @b: second number.
 *
 * Return: product two integer numbers.
 */
int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - divide two integer numbers.
 * @a: first number
 * @b: second number.
 *
 * Return: cocient two integer numbers
 */

int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - calculate reaminder tow integer numbers
 * @a: first number
 * @b: second number.
 *
 * Return: remainder two ineteger numbers.
 */

int op_mod(int a, int b)
{
	return (a % b);
}
